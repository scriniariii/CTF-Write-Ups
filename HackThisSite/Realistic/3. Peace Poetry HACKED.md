
> [!NOTE] 
> From: PeacePoetry  
> Message: I run this website where people can read and submit peace-related poetry. I am doing this out of good will towards others, and I don't see why I would be making enemies out of this, but some real ass hole hacked my website posting a bunch of ignorant aggressive propaganda on the front page. And I made that website a while ago, and I no longer have access to it. Do you think you can hack in and change it back? Please? Oh, and bonus points if you message me the name of the bastard who did this!  
My website can be found [here](https://www.hackthissite.org/missions/realistic/3).

The site's front page has been defaced

check the page source of the defaced version for anything left behind by whoever did this, for some reason attackers leaving comments in their own defacement is more common than it should be

```html
<!--Note to the webmasterThis website has been hacked, but not totally destroyed. The old website is still up. I simply copied the old index.html file to oldindex.html and remade this one. Sorry about the inconvenience.-->
```

The attacker basically handed over the recovery path themselves

Navigating to `oldindex.html` brings back the original, undefaced page, along with the attacker's "manifesto," left in the same location

the goal is to get its content written back into `index.html`

the site has a poetry submission form, and testing how it handles submissions reveals the real vulnerability, the form takes a filename (whatever name the user enters) and writes the submitted content directly to a file with that name, with no validation on the filename itself. Roughly equivalent to

```bash
echo "poem" > HackerManifesto
```

Since the filename is taken as-is from user input, nothing stops it from containing a relative path instead of a plain filename, this is a path traversal via unsanitized file write

Submitting the recovered `oldindex.html` source as the poem content, with the filename field set to break out of the intended upload directory and target the live page directly

```bash
echo "oldindex code" > ../index.html
```

Since the form writes wherever the filename says to, this overwrites the defaced `index.html` with the original page content, restoring the site without needing any credentials or admin access at all